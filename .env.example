# Supabase — kept for reference (chat no longer depends on it)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Gemini API — powers the AI chat via /api/chat (server-side, never exposed)
GEMINI_API_KEY=
# Optional: default is gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash
